[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"
ffi = false

[rpc_endpoints]
base_sepolia = "${BASE_SEPOLIA_RPC_URL}"

# Mainnet RPC is intentionally omitted. Do not add base mainnet here.
