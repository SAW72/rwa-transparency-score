# CoinMarketCap Pro API key — get a free Basic (or hackathon Startup) key at
# https://coinmarketcap.com/api/
# Leave empty (or set USE_FIXTURES=1) to run the demo entirely offline.
CMC_API_KEY=

# Force canned CMC-shaped responses (no network). Default is on when no key is set.
USE_FIXTURES=1
